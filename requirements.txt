# Pinned dependency versions for reproducible builds.
# Update deliberately; run `pip list --outdated` and check CVEs before bumping.
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
httpx==0.28.1
python-dotenv==1.0.1
bcrypt==4.2.1
slowapi==0.1.9
python-jose[cryptography]==3.3.0
gunicorn==23.0.0

